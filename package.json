{
  "name": "vite-rollup",
  "version": "1.0.0",
  "description": "学习rollup",
  "main": "rollup.config.js",
  "scripts": {
    "dev": "rollup --config rollup.config.js --environment NODE_ENV:development --environment mode:local --watch",
    "build": "rollup --config rollup.config.js --environment NODE_ENV:production --environment mode:production"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-json": "^6.1.0"
  },
  "dependencies": {
    "rollup": "^4.44.1"
  }
}
